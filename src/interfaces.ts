interface ICardData {
  data: string | React.ReactNode;
}
interface ICardNode {
  name: string;
  children: string | React.ReactNode;
}
interface IHasElement{
  element: Element
}
interface IChapter {
  title: string;
}